import { createAction } from "redux-actions";

export const SET_ACTIVE_TOURNAMENT = createAction("SET_ACTIVE_TOURNAMENT");
export const DELETE_ACTIVE_TOURNAMENT = createAction(
   "DELETE_ACTIVE_TOURNAMENT"
);
